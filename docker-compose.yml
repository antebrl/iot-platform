version: '3.3'
services:
  iot-gateway:
    build:
      context: ./iot-gateway
    environment:
      - HTTP_SERVER_HOST=http-server
    depends_on:
      - http-server

  http-server:
    build:
      context: ./http-server
    ports:
      - "8080:8080"